var haloColor = (function (exports) {
  'use strict';

  /**
   * @desc 随机生成颜色
   * @return {String}
   */
  function random() {
      return ("#" + ("00000" + ((Math.random() * 0x1000000) << 0).toString(16)).slice(-6));
  }
  /**
   * rgb to hex
   * @param {Number} r
   * @param {Number} g
   * @param {Number} b
   * @return {string} hex
   */
  function rgb2hex(r, g, b) {
      var rgb = b | (g << 8) | (r << 16);
      return "#" + (0x1000000 + rgb).toString(16).slice(1);
  }
  /**
   * hexo to rgb
   * @param {string} hex
   * @return {string} rgb
   */
  function hex2rgb(hex) {
      if (!/^#?([0-9a-fA-F]{3}|[#-9a-fA-F]{6})$/.test(hex))
          return "";
      var str = hex.replace("#", "");
      if (str.length === 3) {
          str = str
              .split("")
              .map(function (v) { return v + v; })
              .join("");
      }
      var n = parseInt(str, 16);
      var r = (n >> 16) & 255;
      var g = (n >> 8) & 255;
      var b = n & 255;
      return r + "," + g + "," + b;
  }

  exports.hex2rgb = hex2rgb;
  exports.random = random;
  exports.rgb2hex = rgb2hex;

  Object.defineProperty(exports, '__esModule', { value: true });

  return exports;

}({}));
